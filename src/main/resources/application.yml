server:
  port: 8080

spring:
  application:
    name: 45133953
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:sk
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true  # 포맷된 SQL 출력
        show_sql: true    # SQL 로그 출력
  servlet:
    multipart:
      max-file-size: 10MB    # 업로드할 파일의 최대 크기
      max-request-size: 10MB # 요청 전체 크기 제한

jwt:
  secret: "kRa/gVjXVrlL4MWT8vKyDe5oFO6E6fMBldObKPKZTxO6MKzYb0Nc56Qgivr6nqGr9SXR3/uTXehmUbeD7QE9ug=="
  expiration: 3600000 # 1시간